version: 2
jobs:
  build:
    docker:
      - image: circleci/node:lts
    steps:
      - checkout
      - run: yarn
      - run: yarn test
      - run:
          name: Deploy to Now
          command: |
            npm i --no-save now
            ./scripts/now-deploy.sh
